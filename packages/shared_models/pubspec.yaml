name: shared_models
description: "Shared data models for the Trevor app."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
