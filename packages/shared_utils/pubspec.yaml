name: shared_utils
description: "Shared utilities for the Trevor app."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
